import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// I usually use the index for the router 
// but since I don't need it for this project I will leave it like this.

ReactDOM.render(
    <App />,
  document.getElementById('root')
);
